<!-- save as public/favicon.svg -->
<svg xmlns="http://www.w3.org/2000/svg"
     width="512" height="512" viewBox="0 0 512 512"
     role="img" aria-label="AS logo">
  <!-- Transparent background -->
  <!-- Text uses South African font (fallbacks included). Increase font-size for stronger presence. -->
  <text x="50%" y="56%" text-anchor="middle" dominant-baseline="middle"
        font-family="'South African', 'Brush Script MT', 'Pacifico', cursive"
        font-weight="700" font-size="400"
        fill="#CC071E">AS</text>
</svg>
